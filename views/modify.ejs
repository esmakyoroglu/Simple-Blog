<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Düzenleme</title>
    <link rel="stylesheet" href="/style/main.css">
</head>
<body>
    <div class="container">
        <h1><%= header %></h1>
        <!--eğer post varsa düzenleme işlemi gerçekleşecek-->
        <% if (locals.post) { %>
            <form id="editForm" method="post" action="api/posts/<%= post.id %>">
                <input type="text" name="title" value="<%= post.title %>">
                <textarea name="content"  cols="30" rows="10"><%= post.content %></textarea>
                <input type="text" name="author" value="<%= post.author %>">
                <button class="full-width" type="submit"><%= submit %></button>
            </form>
        <% } else { %>
            <form id="newPost" method="post" action="api/posts">
                <input type="text" name="title" placeholder="Başlık" required>
                <textarea name="content" placeholder="Yazı" cols="30" rows="10"></textarea>
                <input type="text" name="author" placeholder="Yazar" required>
                <button class="full-width" type="submit"><%= submit %></button>
            </form>
    <% } %>
    </div>
</body>
</html>